# 分账应用设计企划书

## 一、项目概述

### 1.1 项目目标
开发一个移动端友好的分账应用，支持多人分账、灵活调整、智能结算和最少转账步数优化。

### 1.2 技术栈
- **构建工具**: Vite
- **框架**: Vue 3 + TypeScript
- **样式**: Tailwind CSS
- **交互**: 左右滑动切换、垂直滚动

---

## 二、数据结构设计

### 2.1 核心数据模型

```typescript
// 人员模型
interface Person {
  id: string;              // 唯一标识
  name: string;            // 显示名称（默认随机动物名）
  avatar?: string;         // 头像（可选）
}

// 分账记录模型
interface Expense {
  id: string;              // 唯一标识
  payerId: string;         // 付款人ID
  description: string;      // 项目描述
  totalAmount: number;     // 总金额
  createdAt: Date;         // 创建时间
  updatedAt: Date;         // 更新时间
  
  // 分账明细
  splits: ExpenseSplit[];  // 分账列表
}

// 分账明细模型
interface ExpenseSplit {
  personId: string;        // 参与分账的人员ID
  amount: number;          // 该人员应承担金额
  paid: boolean;          // 是否已私下支付（特殊功能）
}

// 结算结果模型
interface SettlementResult {
  personId: string;
  netAmount: number;       // 净金额（正数=应收，负数=应给）
}

// 转账方案模型（最少步数）
interface TransferPlan {
  fromPersonId: string;    // 转出人
  toPersonId: string;      // 转入人
  amount: number;          // 转账金额
}
```

### 2.2 应用状态管理

```typescript
interface AppState {
  people: Person[];                    // 人员列表
  expenses: Expense[];                // 所有分账记录
  currentPersonIndex: number;         // 当前显示的人员索引
}
```

---

## 三、核心算法设计

### 3.1 分账计算算法

#### 3.1.1 基础分账
- **等分**: `amount = totalAmount / splitCount`
- **自定义**: 允许手动调整每个人的金额，但总和必须等于 `totalAmount`

#### 3.1.2 微调算法
当用户调整某个人的金额时：
1. 计算调整差值：`delta = newAmount - oldAmount`
2. 将差值分摊给其他未调整的人（等分或按比例）
3. 确保总和不变

```typescript
function adjustSplit(
  splits: ExpenseSplit[],
  personId: string,
  newAmount: number
): ExpenseSplit[] {
  const split = splits.find(s => s.personId === personId);
  if (!split) return splits;
  
  const delta = newAmount - split.amount;
  const otherSplits = splits.filter(s => s.personId !== personId);
  
  // 将差值分摊给其他人
  const adjustmentPerPerson = -delta / otherSplits.length;
  
  return splits.map(s => {
    if (s.personId === personId) {
      return { ...s, amount: newAmount };
    }
    return { ...s, amount: s.amount + adjustmentPerPerson };
  });
}
```

### 3.2 结算算法

#### 3.2.1 净金额计算
对于每个人，计算：
- **总支付**: 所有该人作为 `payerId` 的 `totalAmount` 之和
- **总承担**: 所有该人参与的 `ExpenseSplit.amount` 之和（排除已私下支付的）
- **净金额**: `netAmount = 总支付 - 总承担`
  - `netAmount > 0`: 应收他人钱
  - `netAmount < 0`: 应给他人钱

```typescript
function calculateNetAmounts(
  people: Person[],
  expenses: Expense[]
): SettlementResult[] {
  return people.map(person => {
    let totalPaid = 0;      // 总支付
    let totalOwed = 0;      // 总承担
    
    expenses.forEach(expense => {
      // 计算总支付
      if (expense.payerId === person.id) {
        totalPaid += expense.totalAmount;
      }
      
      // 计算总承担（排除已私下支付的）
      const split = expense.splits.find(s => s.personId === person.id);
      if (split && !split.paid) {
        totalOwed += split.amount;
      }
    });
    
    return {
      personId: person.id,
      netAmount: totalPaid - totalOwed
    };
  });
}
```

### 3.3 最少转账步数算法 ⭐

这是核心算法，目标是用最少的转账次数完成结算。

#### 3.3.1 问题分析
- 输入：每个人的净金额（应收/应给）
- 输出：转账方案列表，使得转账次数最少

#### 3.3.2 算法思路

**方法一：贪心算法（推荐）**
1. 将所有净金额排序（应收的降序，应给的升序）
2. 每次让最大的应收者和最大的应给者匹配
3. 转账金额 = min(应收金额, |应给金额|)
4. 更新双方余额，重复直到所有人余额为0

```typescript
function calculateMinTransfers(
  settlements: SettlementResult[]
): TransferPlan[] {
  const transfers: TransferPlan[] = [];
  
  // 分离应收和应给
  const creditors = settlements
    .filter(s => s.netAmount > 0)
    .sort((a, b) => b.netAmount - a.netAmount);
  
  const debtors = settlements
    .filter(s => s.netAmount < 0)
    .sort((a, b) => a.netAmount - b.netAmount);
  
  let creditorIdx = 0;
  let debtorIdx = 0;
  
  while (creditorIdx < creditors.length && debtorIdx < debtors.length) {
    const creditor = creditors[creditorIdx];
    const debtor = debtors[debtorIdx];
    
    const transferAmount = Math.min(
      creditor.netAmount,
      Math.abs(debtor.netAmount)
    );
    
    transfers.push({
      fromPersonId: debtor.personId,
      toPersonId: creditor.personId,
      amount: transferAmount
    });
    
    // 更新余额
    creditor.netAmount -= transferAmount;
    debtor.netAmount += transferAmount;
    
    // 移动指针
    if (creditor.netAmount === 0) creditorIdx++;
    if (debtor.netAmount === 0) debtorIdx++;
  }
  
  return transfers;
}
```

**方法二：图论算法（更优但复杂）**
- 将问题转化为有向图，使用最小费用流算法
- 可以进一步优化转账总金额（减少中间转账）

**推荐使用方法一**，因为：
- 实现简单
- 时间复杂度 O(n log n)
- 对于日常分账场景（通常 < 10 人）已经足够高效
- 结果直观易懂

---

## 四、功能模块设计

### 4.1 人员管理模块
- **功能**:
  - 添加人员（默认随机动物名）
  - 删除人员
  - 重命名人员
  - 左右滑动切换人员视图
  - Header显示当前人员名称
  - 人员网格视图（快速跳转和新增）

- **UI组件**:
  - `PersonCard.vue`: 个人卡片组件
  - `AddPersonCard.vue`: 添加人员卡片（加号图标）
  - `PersonSwiper.vue`: 滑动容器组件
  - `AppHeader.vue`: 顶部Header组件
  - `PersonGridView.vue`: 人员网格视图弹窗
  - `PersonGridItem.vue`: 人员网格项组件

### 4.2 记账模块
- **功能**:
  - 创建分账记录
  - 选择付款人
  - 填写项目描述和金额
  - 选择参与分账的人员
  - 微调每个人的金额

- **UI组件**:
  - `ExpenseForm.vue`: 记账表单
  - `SplitAdjuster.vue`: 分账调整器
  - `PersonSelector.vue`: 人员选择器

### 4.3 账目列表模块
- **功能**:
  - 显示该人员相关的所有账目
  - 点击查看详情
  - 编辑账目
  - 标记已私下支付

- **UI组件**:
  - `ExpenseList.vue`: 账目列表
  - `ExpenseItem.vue`: 账目项
  - `ExpenseDetail.vue`: 账目详情/编辑

### 4.4 结算模块
- **功能**:
  - 显示个人净金额（应收/应给）
  - 显示所有人结算结果
  - 显示最少转账方案

- **UI组件**:
  - `SettlementButton.vue`: 结算按钮（底部常驻）
  - `SettlementModal.vue`: 结算弹窗
  - `NetAmountView.vue`: 净金额视图
  - `TransferPlanView.vue`: 转账方案视图

---

## 五、UI/UX 设计

### 5.1 布局结构
```
┌─────────────────────────┐
│  🐱 小猫咪    [👁️]     │  ← Header（当前人员名 + 视图icon）
├─────────────────────────┤
│                         │
│    Person Card View     │  ← 可左右滑动
│    (Full Screen)        │
│                         │
│  ┌─────────────────┐   │
│  │  Animal Name    │   │  ← 可点击改名
│  └─────────────────┘   │
│                         │
│  ┌─────────────────┐   │
│  │  + 记账         │   │  ← 记账板块
│  └─────────────────┘   │
│                         │
│  ┌─────────────────┐   │
│  │  账目1          │   │
│  └─────────────────┘   │
│  ┌─────────────────┐   │
│  │  账目2          │   │  ← 账目列表
│  └─────────────────┘   │
│  ...                    │
│                         │
│  应收: $50.00          │  ← 净金额提示
│                         │
├─────────────────────────┤
│    [结算]               │  ← 底部常驻按钮
└─────────────────────────┘

人员网格视图弹窗（全屏）:
┌─────────────────────────┐
│  选择人员          [✕]  │  ← 弹窗标题 + 关闭按钮
├─────────────────────────┤
│  ┌───┐ ┌───┐ ┌───┐     │
│  │🐱 │ │🐶 │ │🐰 │     │
│  │小  │ │大  │ │兔  │     │  ← 人员网格
│  │猫  │ │狗  │ │子  │     │
│  └───┘ └───┘ └───┘     │
│  ┌───┐ ┌───┐           │
│  │🐼 │ │ + │           │
│  │熊  │ │新  │           │  ← 新增人员
│  │猫  │ │增  │           │
│  └───┘ └───┘           │
└─────────────────────────┘
```

### 5.2 交互流程

1. **添加人员**:
   - 方式一：滑动到空白页 → 点击加号 → 输入名称 → 确认
   - 方式二：点击Header视图icon → 打开人员网格 → 点击"新增"卡片 → 输入名称 → 确认

2. **快速跳转人员**:
   - 点击Header右侧视图icon → 打开人员网格弹窗 → 点击目标人员卡片 → 自动跳转并关闭弹窗

3. **记账流程**:
   - 点击"记账"板块 → 填写表单 → 选择分账人员 → 调整金额 → 保存

4. **查看账目**:
   - 点击账目项 → 查看详情 → 可编辑/标记已支付

5. **结算流程**:
   - 点击结算按钮 → 查看净金额 → 滚动查看转账方案

### 5.3 视觉设计要点
- **卡片式设计**: 每个功能模块使用卡片容器
- **清晰的层次**: 使用阴影和间距区分层级
- **颜色编码**: 
  - 应收：绿色
  - 应给：红色
  - 中性：灰色
- **动画效果**: 滑动、展开/收起使用平滑过渡

### 5.4 Header 设计细节
- **位置**: 固定在顶部，z-index 高于内容区域
- **左侧**: 显示当前人员的动物名称（如 "🐱 小猫咪"）
- **右侧**: 视图icon（👁️ 或网格图标），点击打开人员网格弹窗
- **样式**: 背景色与内容区域区分，有轻微阴影

### 5.5 人员网格视图设计
- **布局**: 响应式网格布局，每行显示2-3个人员卡片
- **人员卡片**:
  - 显示动物emoji和名称
  - 当前人员卡片有高亮边框或背景色
  - 点击卡片跳转到对应人员视图
- **新增卡片**: 
  - 显示大大的加号图标（+）
  - 点击后弹出输入框或表单
- **弹窗行为**:
  - 全屏覆盖（带半透明背景遮罩）
  - 点击遮罩或关闭按钮可关闭
  - 选择人员后自动关闭并跳转

---

## 六、技术架构

### 6.1 项目结构
```
lets-go-dutch/
├── src/
│   ├── components/          # 组件
│   │   ├── Person/
│   │   │   ├── PersonCard.vue
│   │   │   ├── AddPersonCard.vue
│   │   │   ├── PersonSwiper.vue
│   │   │   ├── PersonGridView.vue    # 人员网格视图弹窗
│   │   │   └── PersonGridItem.vue    # 人员网格项
│   │   ├── Expense/
│   │   ├── Settlement/
│   │   └── Layout/
│   │       └── AppHeader.vue         # Header组件
│   ├── views/               # 页面视图
│   │   └── PersonView.vue
│   ├── composables/         # 组合式函数
│   │   ├── usePeople.ts
│   │   ├── useExpenses.ts
│   │   ├── useSettlement.ts
│   │   └── usePersonGrid.ts          # 人员网格视图逻辑
│   ├── utils/               # 工具函数
│   │   ├── algorithms.ts    # 核心算法
│   │   └── animalNames.ts   # 随机动物名
│   ├── types/               # TypeScript 类型
│   │   └── index.ts
│   ├── stores/              # 状态管理（Pinia）
│   │   └── appStore.ts
│   └── App.vue
├── package.json
└── vite.config.ts
```

### 6.2 状态管理方案
使用 **Pinia** 进行状态管理：
- `peopleStore`: 人员管理
- `expenseStore`: 账目管理
- `uiStore`: UI状态（当前人员索引、人员网格弹窗显示状态等）

### 6.3 数据持久化
- 使用 **localStorage** 保存数据
- 或使用 **IndexedDB**（如果数据量大）

---

## 七、实现优先级

### Phase 1: 核心功能
1. ✅ Header组件（显示当前人员名 + 视图icon）
2. ✅ 人员管理（添加、删除、重命名）
3. ✅ 人员网格视图（快速跳转和新增）
4. ✅ 基础记账功能
5. ✅ 账目列表展示

### Phase 2: 高级功能
4. ✅ 分账微调
5. ✅ 结算计算
6. ✅ 最少转账算法

### Phase 3: 优化
7. ✅ 已私下支付标记
8. ✅ UI/UX 优化
9. ✅ 数据持久化

---

## 八、算法复杂度分析

### 8.1 分账计算
- **时间复杂度**: O(n)，n = 分账人数
- **空间复杂度**: O(n)

### 8.2 结算计算
- **时间复杂度**: O(m × n)，m = 账目数，n = 平均分账人数
- **空间复杂度**: O(n)

### 8.3 最少转账算法
- **时间复杂度**: O(n log n)，n = 人员数（排序）
- **空间复杂度**: O(n)

---

## 九、边界情况处理

1. **删除人员**: 如果该人员有未结算的账目，提示用户
2. **金额总和**: 确保分账金额总和等于总金额（允许小数点误差）
3. **空状态**: 没有人员、没有账目时的友好提示
4. **已私下支付**: 标记后，该金额不计入结算计算

---

## 十、后续扩展方向

1. **多币种支持**: 支持不同货币的分账
2. **分组功能**: 支持多个分账组
3. **历史记录**: 查看历史结算记录
4. **导出功能**: 导出结算报告
5. **云端同步**: 多设备数据同步

---

## 总结

本设计文档涵盖了分账应用的核心算法、数据结构和功能模块。重点是最少转账步数算法，采用贪心策略可以在 O(n log n) 时间内找到最优解，满足日常使用需求。

下一步可以开始搭建项目框架和实现核心功能。
